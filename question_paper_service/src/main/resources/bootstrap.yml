server:
  port: 9004

spring:
  application:
    name: question-paper-service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        failFast: false
        enabled: true
        name: oaes
        format: YAML
        data-key: application-config.yml
