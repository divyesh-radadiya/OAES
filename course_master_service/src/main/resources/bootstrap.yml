server:
  port: 9003

spring:
  application:
    name: course-master-service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        failFast: false
        enabled: true
        name: oaes
        format: YAML
        data-key: application-config.yml
