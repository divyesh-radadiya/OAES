server:
  port: 9002

spring:
  application:
    name: item-service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        failFast: false
        enabled: true
        name: oaes
        format: YAML
        data-key: application-config.yml
